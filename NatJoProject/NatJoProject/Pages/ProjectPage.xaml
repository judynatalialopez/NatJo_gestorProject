<Page x:Class="NatJoProject.Pages.ProjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:NatJoProject.Pages"
      Height="700" Width="1200"
      Title="ProjectPage">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="10">
            <TextBlock x:Name="txtNombreProyecto" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="txtDescripcionProyecto" Margin="0,5,0,0"/>
            <TextBlock x:Name="txtFechasProyecto" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Tareas y Miembros -->
            <ScrollViewer>
                <StackPanel>
                    <GroupBox Header="Tareas del Proyecto" FontWeight="Bold" Margin="0,0,0,10">
                        <StackPanel x:Name="PanelTareas" Margin="10"/>
                    </GroupBox>

                    <GroupBox Header="Miembros del Equipo" FontWeight="Bold">
                        <StackPanel x:Name="PanelMiembros" Margin="10"/>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <!-- Agregar miembro -->
            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Agregar miembro por email:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtEmailMiembro" Width="200" Margin="0,0,0,10"/>
                <Button Content="Agregar al equipo" Click="AgregarMiembro_Click" Width="150"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
